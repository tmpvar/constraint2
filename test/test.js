var constraint2;
if (typeof require !== "undefined") {
  constraint2 = require("../constraint2.js");
} else {
  constraint2 = window.constraint2;
}

var ok = function(a, msg) { if (!a) throw new Error(msg || "not ok"); };

describe('constraint2', function() {
  describe('#', function() {
    it('', function() {
      
    });
  });
});
